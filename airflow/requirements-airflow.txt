# Python packages the backend services expect at import time. Keep this in
# rough sync with backend/pyproject.toml — langgraph / langchain-core must
# match the version the agents were written for or graph compilation breaks.

pydantic>=2.7.0
pydantic-settings>=2.2.1
structlog>=24.1.0

# LLM + agent stack (same floors as the backend image).
litellm>=1.83.4
langgraph>=1.1.6
langchain-core>=1.2.28
tenacity>=9.1.4

# Data sources
feedparser>=6.0.12
python-dateutil>=2.9.0.post0
trafilatura>=2.0.0

# Persistence
snowflake-connector-python>=3.10.0
pandas>=2.3.3
pyarrow>=23.0.1
qdrant-client>=1.17.1

# ML / numerical
scikit-learn>=1.7.2
numpy>=2.2.6

# Content parsing
pdfplumber>=0.11.4
pypdf>=6.9.2

# HTTP client used by the ingestion service
httpx>=0.27.0
